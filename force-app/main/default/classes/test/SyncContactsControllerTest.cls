@isTest
public with sharing class SyncContactsControllerTest {
	static testMethod void sendContactsTest() {
        SyncContactsController.sendContactsOrgB();
        //System.assertEquals(/*check for expected results here...*/);
    }
}
